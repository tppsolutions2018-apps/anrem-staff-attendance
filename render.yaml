services:
  - type: web
    name: anrem-staff-attendance
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeployTrigger: commit
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_USER
        sync: false
      - key: ADMIN_PASS
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: anrem-staff-attendance-db
          property: connectionString

databases:
  - name: anrem-staff-attendance-db
    plan: free
